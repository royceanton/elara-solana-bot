import EventEmitter from "eventemitter3";
import { PublicKey } from "@solana/web3.js";
import { UtlConfig } from "./config/utl-config";
import { SearchOptions, UTLOnAccountsLoadedCallback } from './types';
export default class Client extends EventEmitter {
    readonly config: UtlConfig;
    constructor(config?: UtlConfig);
    fetchMint(mint: PublicKey): Promise<import("./types").Token>;
    fetchMints(mints: PublicKey[]): Promise<import("./types").Token[]>;
    searchMints(query: string, options?: SearchOptions): Promise<import("./types").Token[]>;
    getFromTokenList(mints: PublicKey[]): Promise<import("./types").Token[]>;
    getFromMetaplex(mints: PublicKey[], onAccountsLoaded?: UTLOnAccountsLoadedCallback): Promise<import("./types").Token[]>;
}
