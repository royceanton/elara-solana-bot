import { PublicKey } from "@solana/web3.js";
import { SearchOptions, Token } from "../types";
import { UtlConfig } from "../config/utl-config";
export declare const fetchTokensBackend: (config: UtlConfig, mints: PublicKey[]) => Promise<Token[]>;
export declare const searchTokensBackend: (config: UtlConfig, query: string, options: SearchOptions) => Promise<Token[]>;
