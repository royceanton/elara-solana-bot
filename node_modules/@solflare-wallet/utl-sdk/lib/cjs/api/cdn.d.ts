import { Token } from "../types";
import { PublicKey } from "@solana/web3.js";
import { UtlConfig } from "../config/utl-config";
export declare const fetchTokensCdn: ({ cdnUrl }: UtlConfig, mints: PublicKey[]) => Promise<Token[]>;
