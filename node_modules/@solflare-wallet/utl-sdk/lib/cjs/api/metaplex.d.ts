import { PublicKey } from "@solana/web3.js";
import { Token, UTLOnAccountsLoadedCallback } from '../types';
import { UtlConfig } from "../config/utl-config";
export declare const fetchTokensMetaplex: ({ connection, chainId, metaplexTimeout }: UtlConfig, mints: PublicKey[], onAccountsLoaded?: UTLOnAccountsLoadedCallback) => Promise<Token[]>;
